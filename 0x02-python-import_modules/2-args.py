#!/usr/bin/python3
if __name__ == "__main__":
    import sys
    if len(sys.arg) == 1:
        print("0 arguments.")
    else:
        if len(sys.arg) == 2:
            print("1 argument:")
        else:
            print("{} arguments:".format(len(len(sys.arg) -1))
                    a = 0
                    for arr in sys.arg:
                    if a != 0:
                    print("{} : {}".format(a, arr))
                    a = a + 1
