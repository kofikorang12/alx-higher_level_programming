#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    true_orfalse = []
    for i in my_list:
        if i % 2 == 0:
            true_orfalse.append(True)
        else:
            t_orfalse.append(False)
    return t_orfalse
